# Requirements for the FLUX notebook on Google Colab.
# Colab already ships a matching PyTorch + CUDA build, so torch is deliberately
# NOT listed here - reinstalling it costs several minutes and usually breaks CUDA.

diffusers>=0.35.0
transformers>=4.49.0
accelerate>=1.0.0
huggingface_hub>=0.30.0
safetensors>=0.4.5

# T5 tokenizer for the second text encoder
sentencepiece
protobuf

# LoRA support
peft>=0.14.0

# 4-bit NF4 loading, for 16 GB GPUs (T4)
bitsandbytes>=0.45.0

gradio==5.50.0
Pillow
