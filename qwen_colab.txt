# Requirements for the Qwen-Image notebook on Google Colab.
# Colab already ships a matching PyTorch + CUDA build, so torch is deliberately
# NOT listed here - reinstalling it costs several minutes and usually breaks CUDA.

# 0.35 is the first release with QwenImagePipeline; 0.36 adds Qwen-Image-2512.
diffusers>=0.36.0

# Qwen-Image's text encoder is Qwen2.5-VL, whose config needs a recent build.
transformers>=4.53.0
accelerate>=1.0.0
huggingface_hub>=0.30.0
safetensors>=0.4.5

# Lightning LoRAs (and any LoRA on top of a 4-bit model)
peft>=0.15.0

# 4-bit NF4 loading - the default on anything smaller than an 80 GB card
bitsandbytes>=0.45.0

gradio==5.50.0
Pillow
